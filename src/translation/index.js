import en from "./en";
import ar from "./ar";

/**
 * i18next `resources` map: one `translation` namespace per language.
 * Locale JSON files live under `./en` and `./ar`.
 */
export const resources = {
    en: { translation: en },
    ar: { translation: ar },
};
